package gui.MacroCalculator;

import javax.swing.JFrame;

public class MacroWindow extends JFrame {
	/*MacroWindow(String title) {
		super(title);
		setSize(300, 100);
		setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		setSize(300, 500);
		//setLocation(100, 150);
	
		JButton btn1 = new JButton("クリックしてください");
		btn1.addActionListener(new ButtonAbstractAction());
		
		Container cont = getContentPane();
		cont.add(btn1, BorderLayout.SOUTH);
	
	}*/

}

